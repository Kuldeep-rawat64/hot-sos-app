import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kuldeep.myapp',
  appName: 'MyApp',
  webDir: 'dist/hot-sos-app/browser',
};

export default config;
